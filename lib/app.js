
module.exports = {

    views: require('./views'),
    rewrites: require('./rewrites'),
    updates: require('./updates'),
    spatial : require('./spatial'),
    lists: require('./lists')
};

// Bind event handlers
// require('./events')
